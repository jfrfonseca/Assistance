<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="utf-8">
    <title>Gráficos de desempenho de Assistance</title>
	<script src="Chart.js"></script>
  </head>
  <body>
    <canvas id="resPro_strat" width="400" height="400"></canvas>
  </body>
	<script>
	options = {
		scaleBeginAtZero : true,
		scaleShowGridLines : true,
		scaleGridLineColor : "rgba(0,0,0,.05)",
		scaleGridLineWidth : 1,
		scaleShowHorizontalLines: true,
		scaleShowVerticalLines: true,
		barShowStroke : true,
		barStrokeWidth : 2,
		barValueSpacing : 5,
		barDatasetSpacing : 1,
		legendTemplate : "<ul class=\"<%=name.toLowerCase()%>-legend\"><% for (var i=0; i<datasets.length; i++){%><li><span style=\"background-color:<%=datasets[i].fillColor%>\"></span><%if(datasets[i].label){%><%=datasets[i].label%><%}%></li><%}%></ul>"
	}
	/*
	AVERAGE RESPONSE/PROCESSING TIME PER SCHEDULE STRATEGY
	*/
	var ctx = document.getElementById("resPro_strat").getContext("2d");
	var TresplTproc_strat = new Chart(ctx).Bar(TresplTproc_strat_data, options);
	</script>
</html>
