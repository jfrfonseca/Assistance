import psutil
	
def getCPUusage()
	return psutil.cpu_percent(interval=1, percpu=True)

def getMemoryUsage()
	mainmem = psutil.phymem_usage()
    virtmem = psutil.virtmem_usage()
    return [mainmem.percent, virtmem.percent, swapmem.percent]


